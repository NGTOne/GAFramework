//This is an abstract class for fitness functions - should be extended as necessary, with one
//subclass per "species" of individual

class FitnessFunction {
	private:

	protected:

	public:
	int checkFitness(GenePool[] pools, int[] indexes);
}
